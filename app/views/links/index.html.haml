%h1
  = "Please enter your link in form below"

= form_tag("/convert", method: "get") do
  = text_field_tag(:q)
  = submit_tag("Convert")

-#= form_for @links do |f|
-#  .field
-#    = f.text_field :origin
-#  .actions
-#    = f.submit


= form_tag '/links' do
  .form-group.row
    .col-xs-3
      %input.form-control{type: 'text', name: 'origin', placeholder: 'Origin'}

  = submit_tag 'Save', class:'btn btn-success'